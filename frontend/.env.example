# Gateway routes auth and content through http://localhost:8000 by default.
VITE_API_BASE_URL=http://localhost:8000

# Use this override when the gateway is unavailable but content-service is running directly.
# VITE_CONTENT_API_URL=http://localhost:8082